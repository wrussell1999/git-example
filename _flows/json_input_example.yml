id: json_input_example
namespace: example

inputs:
  - id: data
    type: JSON
    defaults: |
      {"name": "kestra", "rating": "best in class"}

tasks:
  - id: hello
    type: io.kestra.plugin.core.log.Log
    message: "{{ inputs.data }}"